import 'package:flutter/material.dart';

class TopperListImageScreen extends StatelessWidget {
  const TopperListImageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Topper List Images'),
      ),
    );
  }
}
