import 'package:flutter/material.dart';

class SubjectTopperImageTable extends StatelessWidget {
  const SubjectTopperImageTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
